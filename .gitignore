# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.waylog

# Dependencies
node_modules
dist
dist-ssr
*.local

# Environment (nunca commitear credenciales)
.env
.env.*
!.env.example

# Capturas del CRM viejo (contienen credenciales/tokens reales — nunca commitear)
scraping/

# Inputs locales de scripts (contraseñas de seed, etc. — nunca commitear)
scripts/*.local.json

# Editor / OS
.DS_Store
.vscode/*
!.vscode/extensions.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?



scrap/.waylog/
